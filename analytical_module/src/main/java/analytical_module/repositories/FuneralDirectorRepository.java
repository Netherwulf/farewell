package analytical_module.repositories;

import analytical_module.models.FuneralDirector;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FuneralDirectorRepository extends JpaRepository<FuneralDirector, Long> {
}
